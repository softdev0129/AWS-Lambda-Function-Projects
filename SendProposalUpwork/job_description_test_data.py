sample_text = """We had a deployed RAG application running on an AWS instance
However

Suddenly we started running into problems regarding SSL certificates

We started playing around trying to install it ourselves and I guess we ended up complicating things further

We have the original code backed up. So all we want is someone to test it and re deploy it the proper way on our AWS instance """